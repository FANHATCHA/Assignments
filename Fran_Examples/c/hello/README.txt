
This directory contains a very simple C program, as well as the makefile
required to build it.  To build, simply type:
	make

The results of this will be an executable file called "hello".  You can
run the file (regardless of your $PATH setting) by typing:
	./hello

